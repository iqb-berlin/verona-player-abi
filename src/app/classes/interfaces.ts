export enum FieldType {
  UNSET,
  TEXT,
  HEADER,
  TITLE,
  INPUT_TEXT,
  INPUT_NUMBER,
  HTML,
  HR,
  CHECKBOX,
  MULTIPLE_CHOICE,
  DROP_DOWN,
  SCRIPT_ERROR,
  NAV_BUTTON_GROUP,
  LIKERT_ELEMENT,
  LIKERT_BLOCK,
  IFTHENELSE_BLOCK,
  REPEAT_BLOCK,
  SIMPLE_BLOCK
}

export enum NavButtonOptions {
  'previous',
  'next',
  'first',
  'last',
  'end'
}
